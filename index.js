/**
 * @format
 */

import {AppRegistry} from 'react-native';
import { App } from './App';
import {name as FlipTransactionApp} from './app.json';

AppRegistry.registerComponent(FlipTransactionApp, () => App);
